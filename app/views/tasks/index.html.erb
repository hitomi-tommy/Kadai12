<h1>タスク一覧</h1>
<table>
  <tr>
    <th>タスク名</th>
    <th>作成日時</th>
  </tr>
<% @task.each do |task| %>
  <tr>
    <td><%= task.name %></td>
    <td><%= task.created_at %></td>
    <td><%= link_to '詳細を確認する', task_path(task.id) %></td>
  </tr>
<% end %>
</table>
<%= link_to '新規作成', new_task_path %>
